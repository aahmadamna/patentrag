[build]
builder = "nixpacks"

[deploy]
startCommand = "cargo run"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[phases.setup]
nixPkgs = ["poppler_utils", "tesseract", "tesseract-data-eng"] 